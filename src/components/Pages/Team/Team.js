import React from "react";
import preloader from '../../../assets/img/preloader.gif'

const Team = () => (
    <section className="wow fadeIn content-details" style={{height: '100vh'}}>
        <img src={preloader} alt="preloader" style={{width: 250}}/>
    </section>
);


export default Team;
